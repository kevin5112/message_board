<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/styles.css" type="text/css" />
    <title><%= title %></title>
  </head>
  <body>
    <div class="main">
      <div class="container">
        <% for( let i = 0; i < messages.length; i++ ) { %>
        <div class="entry">
          <p><%= messages[i].user %></p>
          <p><%= messages[i].text %></p>
          <p><%= messages[i].added %></p>
        </div>
        <% } %>
      </div>
      <button action="/new">Add new</button>
    </div>
  </body>
</html>
